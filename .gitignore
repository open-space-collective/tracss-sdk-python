# Python / uv
.venv/
__pycache__/
*.egg-info/
dist/
*.pyc
.coverage
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Fern
**/.preview/
**/.definition/
# Regenerated each run with non-deterministic provenance (origin git commit,
# invokedBy manual/ci); the meaningful config lives in fern/generators.yml.
sdks/python/tracss/.fern/metadata.json

# Local secrets
.env

# Prism mock server PIDs (written by make prism-all, cleaned up by make prism-stop)
.prism.pids
*.html
test.py

# Claude stuff
.claude/
CLAUDE.md
